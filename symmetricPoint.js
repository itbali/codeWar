function symmetricPoint(p, q) {
    let pd = [];

    if (p[0] - q[0]) {
        pd[0] = p[0] - q[0];
    } else {
        pd[0] = p[0] - q[0];
    }

    if (p[1] - q[1]) {
        pd[1] = p[1] - q[1];
    } else {
        pd[1] = p[1] - q[1];
    }
    console.log(p);
}
